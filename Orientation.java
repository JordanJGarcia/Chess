package chess;                                                                                                                                                                         

// this allows for custom colors
// as well as determining the board orientation
// when generating moves
enum Orientation {
    NON,    // for default constructors
    WHITE,  // whites turn, 0 is at square a8
    BLACK   // blacks turn, 0 is at square h1
}

